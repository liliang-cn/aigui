---
"@ai-gui/plugin-graph": minor
---

New package: knowledge graphs and ontologies (` ```graph `) — entities and typed relations, classes with `subClassOf` and properties with `domain`/`range`, drawn as 2D SVG or a three.js model, with the relations that break the ontology marked.
