---
"@ai-gui/plugin-bigscreen": patch
---

A `GlobeSkin` can set `light.ambient`, how much the night side still shows. The default stays 0.5; a host whose photograph has dark oceans raises it instead of watching half the planet go black at dusk.
