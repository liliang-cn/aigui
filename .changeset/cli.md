---
"@ai-gui/cli": minor
---

New package: the `aigui` command line. `aigui prompt` writes the system prompt `buildSystemPrompt` would build, from a JSON config or flags, so a Go, Rust, Python or Java backend can read it from a file instead of receiving it from the browser.
