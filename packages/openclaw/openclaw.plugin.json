{
  "id": "ai-gui",
  "name": "AIGUI",
  "description": "Renders charts, diagrams, math, tables, cards, dashboards, 3D scenes, orbits, data walls and molecules as images for picture-only channels.",
  "activation": {
    "onStartup": true
  },
  "contracts": {
    "tools": [
      "aigui_render"
    ]
  },
  "toolMetadata": {
    "aigui_render": {
      "optional": true
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "channels": {
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "blocks": {
        "type": "array",
        "items": {
          "enum": [
            "chart",
            "mermaid",
            "dashboard",
            "card",
            "math",
            "table",
            "scene",
            "gravity",
            "bigscreen",
            "molecule"
          ]
        }
      },
      "theme": {
        "enum": [
          "light",
          "dark"
        ]
      },
      "width": {
        "type": "integer",
        "minimum": 200,
        "maximum": 2000
      },
      "scale": {
        "type": "integer",
        "minimum": 1,
        "maximum": 4
      },
      "maxImages": {
        "type": "integer",
        "minimum": 1,
        "maximum": 20
      },
      "timeoutMs": {
        "type": "integer",
        "minimum": 1000,
        "maximum": 120000
      },
      "idleShutdownMs": {
        "type": "integer",
        "minimum": 10000,
        "maximum": 3600000
      }
    }
  }
}
