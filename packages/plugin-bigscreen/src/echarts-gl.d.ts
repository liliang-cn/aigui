// `echarts-gl` ships no type declarations. It is imported only for its side effect (registering
// the WebGL renderer and the 3D chart types on echarts), so an ambient module is sufficient.
declare module "echarts-gl"
