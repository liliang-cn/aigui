/**
 * KaTeX ships the mhchem extension without types.
 *
 * It is imported for its side effect — it registers the `\ce{}` and `\pu{}` grammar with KaTeX — so
 * there is nothing to describe beyond the module existing.
 */
declare module "katex/contrib/mhchem"
